import 'package:flutter/material.dart';

abstract class AppColors {
  static const Color primary = Color(0xFF0A9F68);
  static const Color selectedPrimary = Color(0xFF088255);
  static const Color secondary = Color(0xFFFBF8CC);
  static const Color error = Colors.redAccent;
}
